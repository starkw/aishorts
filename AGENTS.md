# 协作约定

## 发布流程

改动完成并自检通过后，**直接提交并推送到生产**，不用再问「要不要发生产」。

自检指本地跑通以下项，任一不过就先修再发：

- `npx tsc --noEmit` 类型检查通过
- `npm run build` 构建通过
- 涉及版式的改动，渲染核对桌面与移动端（移动端至少确认无横向溢出、无内容裁切）

只有以下情况需要先问：

- 改动会删除线上已有功能或页面
- 需要改动环境变量、密钥或第三方服务配置
- 有多个方案且取舍影响产品方向

## 参考站复刻

复刻其他站点的版式时，只对齐结构与视觉（布局、配色、组件形态、内容规模与分类体系），
文案、描述与提示词一律自行编写，不照搬对方的文本内容。
